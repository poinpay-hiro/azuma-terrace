# build output
/dist/

# dependencies (none required, but ignore if added later)
node_modules/

# OS / editor
.DS_Store
*.log
.claude/

# never commit design source PDFs or secrets
*.pdf
.env
.env.*

# push関所の通行証（ローカル限り・1回限り。CLAUDE.md §3）
.review-approved
